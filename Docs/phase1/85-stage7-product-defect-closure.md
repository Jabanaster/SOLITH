# Phase 1 / Stage 7 — Product Defect Closure Matrix

## §7.25 — explicit, unambiguous status

**Production scanner routing default is `LEGACY` for every session, unconditionally.** This alone means no shipping-product defect can be honestly reported CLOSED this stage — mission §7.10-§7.13 each require "shipping production routing for affected scan operations uses native" as a precondition, and that precondition is false by design (canary has not passed; see doc 76).

| Defect | Status | Evidence |
|---|---|---|
| **1 MiB (D02)** | **OPEN** | Native-path fix re-confirmed real (doc 76, item 1: real 4 MiB region, real sentinel beyond 1 MiB found by native, missed by legacy). Shipping routing still LEGACY by default — `PRODUCT_DEFECT_NOT_YET_CLOSED` |
| **Alignment (D03)** | **OPEN** | Native-path fix re-confirmed real (doc 76, item 2: real unaligned offset 449, found by native via bytewise mode, missed entirely by legacy's type-width-stride `scanFirst`). Shipping routing still LEGACY by default — `PRODUCT_DEFECT_NOT_YET_CLOSED` |
| **AOB (D01/D04)** | **OPEN** | Native-path fix re-confirmed real (doc 76, item 3) for `aob-resolver.ts`'s specific code path ONLY. `signature-engine.ts`'s independent AOB-shaped implementation (doc 83) is a SECOND, unmigrated instance of the same underlying defect — even if `aob-resolver.ts`'s path were switched to native, the AOB defect as a whole would still not be closed. Shipping routing still LEGACY by default — `PRODUCT_DEFECT_NOT_YET_CLOSED` |
| **int64 (D06)** | **OPEN** | Native-path fix re-confirmed real (doc 76, item 2: exact `valueBigint` for a real value beyond `Number.MAX_SAFE_INTEGER`). Additionally, a related, previously-undocumented real defect was found this stage: legacy's `encodeValue` throws an UNCAUGHT `RangeError` for an int64 search value whose `Number` narrowing rounds to or past 2^63 (`scanner-backend-legacy.test.ts`'s "int64 above Number.MAX_SAFE_INTEGER" test discovered this; `LegacyScannerBackend` now catches it and surfaces a typed error instead of letting it crash the caller — but the underlying `memory-scanner.ts`/`encodeValue` function itself is unmodified and would still crash any OTHER caller that doesn't go through this stage's adapter). Shipping routing still LEGACY by default — `PRODUCT_DEFECT_NOT_YET_CLOSED` |
| **Pointer depth/truncation (D05)** | **OPEN, untouched** | Neither Stage 5, Stage 6, nor this stage touched pointer-scanning code. `pointer-scanner.ts`/`pointer-resolver.ts` are unmodified. Retains doc 44's original "Not yet closed" (P1-SCAN-001) wording exactly — not accidentally closed by any collateral scanner work this stage |
| **Sibling truth-reporting legacy defects** | **OPEN** | The legacy path (`memory-scanner.ts`'s `scanFirstAutoMatrix`/`scanNext`/`scanFirstUnknown`/etc., `signature-engine.ts`, `pointer-scanner.ts`) remains shipping-authoritative for every operation this stage did not route. Additionally, a broader instance of the truth-reporting gap was reconfirmed this stage: `scanFirst`'s own `truncated` flag never reflects a region skipped due to a `readBuffer` throw (only `maxTotalBytes`/`maxMatches` limits set it) — `LegacyScannerBackend` compensates for this independently (an honest completeness signal added at the adapter layer, doc 73), but `memory-scanner.ts`'s own `ScanResult.truncated` field, as returned by every OTHER (non-routed) caller of `scanFirst`, is unchanged and still capable of under-reporting |

**No defect is closed this stage.** All four named shipping-product defects, plus D05 and the sibling truth-reporting defects, remain explicitly `PRODUCT_DEFECT_NOT_YET_CLOSED` / `OPEN`. What this stage delivers instead is real, evidence-backed infrastructure and real-process proof that the native path correctly fixes each one — the actual closure is Stage 8's stated objective (production routing switch + full parity + shipping-test coverage + old implementation no longer authoritative), consistent with `NATIVE_PATH_FIXED != PRODUCT_DEFECT_CLOSED` (Stage 6.1's own explicit rule, doc 71, reapplied identically here).
