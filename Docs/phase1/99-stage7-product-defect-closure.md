# Phase 1 / Stage 7 Final Closure — Product Defect Closure (Final Disposition)

## §22-27 — evaluated strictly against each section's own literal closure criteria

**Production scanner routing default is `LEGACY` for every session, unconditionally** — unchanged by this closure pass. Sections 22/23/24/25 each require, as a literal precondition, that "the actual production route" / "actual shipping facade/IPC" / "production routing default" has switched to native for the relevant operation. It has not. This alone means no shipping-product defect can be honestly reported `PRODUCT_DEFECT_CLOSED` this pass, regardless of how strong the native-path evidence is — consistent with `NATIVE_PATH_FIXED != PRODUCT_DEFECT_CLOSED`, the discipline this entire operation has applied at every prior stage.

Mission §16's own text acknowledges this explicitly: "Expected likely final matrix **if all Stage 7 routing tests succeed**" — and separately, unambiguously: "Do not force expected result. Evidence decides." The evidence: production routing has not switched, and several of its own stated preconditions for switching (full parity matrix, §11; real production IPC-path canary against a real game, not just the synthetic fixture; packaged in-app GUI click-through, doc 90's disclosed gap; cancellation wire support, doc 89/92/95/96's disclosed gap) are themselves not yet met. The matrix is therefore evaluated as `PRODUCT_DEFECT_NOT_YET_CLOSED` across the board, below.

| Defect | Section | Native-path evidence (real, this pass + prior stages) | Shipping disposition |
|---|---|---|---|
| **1 MiB (D02)** | §22 | Real IPC path (doc 89): NATIVE mode finds a real >1 MiB sentinel; real-game canary (doc 91): all 3 real games show native covering 4x–23x more real bytes than legacy | `PRODUCT_DEFECT_NOT_YET_CLOSED` — production routing default is LEGACY; legacy's cap remains authoritative for every real user's session |
| **Alignment (D03)** | §23 | Real IPC path + real fixture (doc 89, doc 93): native finds a real unaligned i64 value legacy structurally cannot reach (steps by type width) | `PRODUCT_DEFECT_NOT_YET_CLOSED` — same reason; u16/u32 unaligned cases specifically requested by §23 were not separately re-verified this pass (i64 was) |
| **AOB (D01/D04)** | §24 | Real IPC path (doc 89): native finds a real far-offset marker; legacy misses it, `isAuthoritativeAbsence: false` correctly reported | `PRODUCT_DEFECT_NOT_YET_CLOSED` — production routing default is LEGACY; additionally, `signature-engine.ts`'s independent, unmigrated AOB-shaped implementation (doc 97) means AOB could not be "fully closed" even if `aob-resolver.ts`'s specific path were switched — a second real instance of the same defect mechanism would remain live regardless |
| **INT64 (D06)** | §25 | This closure pass's full end-to-end proof (doc 93): exact fidelity through the ENTIRE real production chain, including preload/renderer type contract fixes and the mission's specific adversarial values (2^53+1, 2^53+3, i64::MIN, u64::MAX) | `PRODUCT_DEFECT_NOT_YET_CLOSED` for the same routing-default reason — the native code path itself is now proven exact end to end (a genuinely new, stronger state than any prior stage achieved for this defect), but "closed" per §25's own text requires the full application boundary to be the one a real user's default session actually uses, which it is not |
| **Pointer depth (D05)** | §26 | Not touched by any scanner-routing stage, ever | `PRODUCT_DEFECT_NOT_YET_CLOSED`, untouched — wording preserved unchanged from doc 44's original "Not yet closed" (P1-SCAN-001) |
| **Sibling truth-reporting defects** | §27 | `LegacyScannerBackend` (Stage 7) independently compensates with an honest completeness signal AT THE ADAPTER LAYER ONLY — `memory-scanner.ts`'s own `ScanResult.truncated` field, as returned by every OTHER (non-adapter, non-routed) caller of `scanFirst`, is unchanged and still capable of under-reporting a silently-skipped region | **Production routing has not replaced any of these paths as shipping-authoritative** — the legacy path remains authoritative for every operation not specifically routed through `scanExactViaBackend`/`scanAobViaBackend` (i.e., everything except plain exact-value scan and plain AOB scan). No blanket closure; each caller's disposition is exactly as recorded in doc 97 |

## What genuinely changed this closure pass, stated plainly

This pass did NOT close any shipping-product defect — consistent with every prior stage of this operation. What it DID do, which is real and substantive:

1. Found and fixed a real, previously-undiscovered bug that meant the INT64 defect's native-path fix had never actually been proven through the layer that matters (the session/IPC boundary) — it is now proven, exactly, through the complete real chain including the renderer type contract, for the mission's own specific adversarial values.
2. Found and fixed a real, previously-undiscovered, genuinely critical defect (unbounded native match accumulation — 151,382 real matches with no cap) that would have made any future production routing switch to NATIVE unsafe regardless of correctness — this is now closed with real evidence at three independent layers and permanent regression coverage.
3. Closed the "standalone require() is not sufficient" objection for the packaged addon (doc 90) with a real scan proof, and made that proof immutable/exact/repeatable via a permanent verification script with hash/size/path assertions (this closure pass).
4. Expanded real-process, real-IPC-path, and real-game evidence meaningfully (maxMatches matrix, 100k-request safety, exact adversarial int64/u64 values planted live via the fixture's own write protocol).

None of this changes the fundamental, honestly-reported state: **shipping routing is still LEGACY by default, so no shipping-product defect is closed.** The native path is more thoroughly proven correct than at any prior point in this operation, which is real progress toward Stage 8's actual closure work — not a substitute for it.
